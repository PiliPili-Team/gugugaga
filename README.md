1. Add client_secret.json from Google Cloud Console to this directory.

2. Run this program first at local environment to generate token

3. Copy the `config dir` (from https://crates.io/crates/dirs) to your server environment.
